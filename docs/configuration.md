# Obsoleta – consulta `docs/configurazione.md`

Le linee guida sulla separazione tra `.env`, `config/config.yaml` e le nuove impostazioni
OIDC sono state spostate in [docs/configurazione.md](configurazione.md).

Aggiorna i link esistenti per puntare al nuovo documento.
