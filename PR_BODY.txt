Adds standard labels (deps:security, deps:tooling, deps:runtime, maintenance)

Sync labels via workflow

Auto-label Dependabot PRs

Enable auto-merge only for deps:tooling after required checks pass

Note: Auto-merge works only if branch protection on main requires ci / tests as required status check
