# Esempio di variabili per configurazione runtime (v1.2.0)

# Google Drive (solo per pre_onboarding con Drive)
DRIVE_ID=your_drive_id_here
SERVICE_ACCOUNT_FILE=path/to/service_account.json

# GitHub (solo per push in onboarding_full)
GITHUB_TOKEN=your_github_token
GIT_DEFAULT_BRANCH=main

# GitBook (non più gestito direttamente, ma mantenuto per retrocompatibilità)
GITBOOK_TOKEN=your_gitbook_token
GITBOOK_WORKSPACE=your_workspace

# Logging e comportamento runtime
LOG_PATH=logs/onboarding.log
LOG_LEVEL=INFO
LOG_REDACTION=auto   # auto | on | off
ENV=dev              # dev | prod | ci
CI=false             # true/false

# Override opzionale file YAML per struttura raw
YAML_STRUCTURE_FILE=config/cartelle_raw.yaml
