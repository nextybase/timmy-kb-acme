# requirements.txt
# =============================================================================
# Pin statici per la pipeline NeXT Timmy-KB
#
# Obiettivo: build ripetibili e sicure. Le versioni qui sotto sono **pinnate**
# (==) in linea con le annotazioni: niente "drift" dovuto ad aggiornamenti a
# sorpresa delle dipendenze.
#
# Note:
# - Se aggiorni una versione, aggiorna e committa l'intero file.
# - Alcune componenti (es. spaCy) richiedono risorse extra post-install.
#   Vedi il blocco in fondo.
# =============================================================================

# === Librerie principali per la pipeline NeXT Timmy-KB ===
PyMuPDF==1.26.5
# pypdf e il successore mantenuto di PyPDF2 (che non riceve piu update)
pypdf==6.1.1
pydantic==2.7.1
pydantic-settings==2.0.0
python-dotenv==1.0.1
python-slugify==8.0.4
PyYAML==6.0.1
requests==2.32.2
google-api-python-client==2.171.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0
spacy==3.7.4
docker==7.0.0
fpdf==1.7.2
PyGithub==2.3.0

# === Componenti rendering/reportistica ===
reportlab==4.4.4

# === Utility e compatibilità ===
typing-extensions==4.12.2
annotated-types==0.7.0
hypothesis==6.112.2

# Libreria opzionale per l'interfaccia utente (Streamlit)
streamlit==1.50.0

# === RAG / embeddings ===
openai==2.3.0
# httpx >=0.28: allinea le API usate nel codice (Timeout.pool, ecc.)
httpx==0.28.1
tiktoken==0.7.0
# NumPy 2.x testata con torch 2.8 e scikit-learn 1.7.x
numpy==2.3.0

# Scarica il modello italiano piccolo per spaCy:
# py -m spacy download it_core_news_md

# Dipendenze Drive
google-auth==2.34.0
httplib2==0.22.0
requests-oauthlib==2.0.0

# === NLP keywords extraction ===
sentence-transformers==2.7.0
# 1.7.2: bugfix + supporto Python 3.14 (richiede scipy>=1.8; pip la risolve da solo)
scikit-learn==1.7.2
yake==0.4.8
torch==2.8.0
