# Timmy‑KB — .env.example
# -----------------------------------------------------------------------------
# Copia questo file in `.env` e compila SOLO le variabili necessarie
# per il tuo scenario. I percorsi possono essere assoluti o relativi al repo.
# Le stringhe con spazi NON vanno tra virgolette.
# -----------------------------------------------------------------------------

# Ambiente
ENV=dev
CI=false
# Redazione automatica dei log: auto|on|off
LOG_REDACTION=auto

# -----------------------------------------------------------------------------
# OpenAI (Vision → YAML e funzioni AI)
# Usa **UNO** dei due meccanismi:
# 1) API key diretta
# 2) Cartella con più key (selezione per modello/ambito)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-...
# oppure
# OPENAI_API_KEY_FOLDER=.keys

# (Opzionale) API key per il coder/RAG della UI; se assente si usa OPENAI_API_KEY
# OPENAI_API_KEY_CODEX=sk-...

# -----------------------------------------------------------------------------
# Google Drive (necessario se usi il default Drive per Tag Onboarding/gestione RAW)
# -----------------------------------------------------------------------------
# Percorso al JSON del Service Account
# SERVICE_ACCOUNT_FILE=secrets/service-account.json
# ID della cartella root (Shared Drive o My Drive) dove creare <slug>/
# DRIVE_ID=0AAbcdefGHIJKlmnPQRstuVwxYZ

# (Opzionale) Timeout richieste Drive (secondi)
# DRIVE_HTTP_TIMEOUT=60

# -----------------------------------------------------------------------------
# GitHub Push (Onboarding Full)
# -----------------------------------------------------------------------------
# GITHUB_TOKEN=ghp_...
# GIT_DEFAULT_BRANCH=main
# (Opzionali) Identità git per i commit
# GIT_USER_NAME=timmy-bot
# GIT_USER_EMAIL=timmy-bot@example.org

# -----------------------------------------------------------------------------
# UI Admin (Google OAuth) — solo se attivi il login da pagina admin
# -----------------------------------------------------------------------------
# GOOGLE_CLIENT_ID=...
# GOOGLE_CLIENT_SECRET=...
# (Consigliato) Restringi al dominio consentito (es. unisom.it)
# GOOGLE_ALLOWED_HD=unisom.it
# (Opzionale) TTL sessione (secondi)
# UI_SESSION_TTL_SECONDS=86400

# -----------------------------------------------------------------------------
# Opzioni pipeline (avanzate)
# -----------------------------------------------------------------------------
# YAML alternativo per la struttura raw (pre-onboarding)
# YAML_STRUCTURE_FILE=config/cartelle_raw.yaml
# Modalità di anteprima (Docker); se Docker manca la UI degrada automaticamente
# PREVIEW_HONKIT_IMAGE=node:20-bookworm

# -----------------------------------------------------------------------------
# Debug/Dev
# -----------------------------------------------------------------------------
# Abilita trace esteso in alcuni componenti (valori: 0|1)
# DEBUG_DRIVE=0
# DEBUG_SEMANTIC=0
