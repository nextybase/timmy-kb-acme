# Example environment variables for Timmy-KB (root)

# Google Drive (optional; used by pre_onboarding/tag_onboarding when source=drive)
DRIVE_ID=
SERVICE_ACCOUNT_FILE=

# GitHub (optional; used by onboarding_full push)
GITHUB_TOKEN=
GIT_DEFAULT_BRANCH=main

# Logging and runtime
LOG_REDACTION=auto   # auto | on | off | always | never
ENV=dev              # dev | prod | ci
CI=false             # true/false

# Optional override for RAW folders structure
YAML_STRUCTURE_FILE=config/cartelle_raw.yaml

# Governance (optional): allowlist for force pushes
GIT_FORCE_ALLOWED_BRANCHES=main,release/*

