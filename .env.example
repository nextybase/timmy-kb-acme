# =====================================================
#           ENV CONFIG – Pipeline Onboarding NeXT
#         Rename this as ".env" and fill in your values
# =====================================================

# === Google Drive ===
DRIVE_ID=YOUR_GOOGLE_DRIVE_ID                  # e.g. 0BRTQ0HoCuSCNUk9TRE
SERVICE_ACCOUNT_FILE=service_account.json      # Path to your Google API credentials

# === GitHub ===
GITHUB_ORG=your-org-or-username                # GitHub org or username for repo creation
REPO_CLONE_BASE=/path/to/your/local/repos      # Base local path for repo clones (e.g. ./repos)
REPO_VISIBILITY=private                        # 'private' or 'public'

# === Pipeline / Folder Templates ===
CARTELLE_RAW_YAML=config/cartelle_raw.yaml         # Path to folder structure YAML
LOCAL_TEMP_CONFIG_PATH=temp_config/config.yaml     # Temporary config path

# === Dynamic paths for ingest pipeline ===
RAW_DIR_TEMPLATE={base_drive}/{slug}/raw           # Template for Drive input path
OUTPUT_DIR_TEMPLATE=output/timmy_kb_{slug}         # Template for output path
BASE_DRIVE=/path/to/your/shared/drive              # Local mount path or base path

# === Docker Preview ===
GITBOOK_IMAGE=honkit/honkit                        # Docker image for GitBook preview

# === GitBook API (optional) ===
GITBOOK_TOKEN=your_gitbook_token_here
GITBOOK_WORKSPACE=your_gitbook_workspace

# === Advanced options (optional) ===
# LOG_FILE=logs/onboarding.log                     # (optional) Log file

# === Example: for future extensions ===
# SLACK_WEBHOOK_URL=
# EMAIL_NOTIFY=

# =====================================================
#   ⚠️  NEVER commit your real ".env" or secrets to public repos!
# =====================================================
