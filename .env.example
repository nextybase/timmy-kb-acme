# === GOOGLE DRIVE ===
SERVICE_ACCOUNT_FILE=service_account.json         # Percorso al file di credenziali JSON del Service Account
DRIVE_ID=xxx...xxx                                # ID del Drive Condiviso dove creare le cartelle dei clienti

# === STRUTTURA LOCALE ===
LOCAL_TEMP_CONFIG_PATH=temp_config/config.yaml    # Path locale per il file config.yaml temporaneo
CARTELLE_RAW_YAML=config/cartelle_raw.yaml        # Template struttura cartelle cliente

# === DOCKER (per anteprima GitBook) ===
GITBOOK_IMAGE=honkit/honkit                       # Immagine Docker usata per anteprima GitBook

# === GITHUB ===
GITHUB_ORG=nextybase                              # Organizzazione o utente GitHub proprietario dei repo
REPO_VISIBILITY=private                           # Visibilità repo: 'private' o 'public'

# === PERCORSI TEMPLATE ===
RAW_DIR_TEMPLATE={base_drive}/{slug}/raw
OUTPUT_DIR_TEMPLATE=output/timmy_kb_{slug}
BASE_DRIVE=G:/Drive condivisi/Nexty Docs           # Base path usato nei template (solo documentazione interna)

# === ALTRE VARIABILI OPZIONALI ===
REPO_CLONE_BASE=.                                 # Dove clonare o creare le repo localmente
