# =====================================================
#           ENV CONFIG – Pipeline Onboarding NeXT
#   Rinominare in ".env" e compilare i campi obbligatori
# =====================================================

# === Google Drive ===
DRIVE_ID=0ADCU0HoCuSCNUk9PVA                      # ID del Drive condiviso clienti
SERVICE_ACCOUNT_FILE=service_account.json         # File credenziali Google API (aggiungi se diverso)

# === GitHub ===
GITHUB_ORG=nextybase                              # Nome dell'utente GitHub
REPO_CLONE_BASE=C:/Users/User/clienti/timmy-kb-acme/output  # Path base per i repo locali
REPO_VISIBILITY=private                           # Opzionale: 'private' o 'public'
GITHUB_TOKEN=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

# === Cartelle Pipeline / Template ===
CARTELLE_RAW_YAML=config/cartelle_raw.yaml        # YAML struttura cartelle (personalizza se serve)
LOCAL_TEMP_CONFIG_PATH=temp_config/config.yaml    # File temporaneo di config locale

# === Path dinamici per ingest pipeline ===
RAW_DIR_TEMPLATE={base_drive}/{slug}/raw          # Template path input (personalizzabile)
OUTPUT_DIR_TEMPLATE=output/timmy-kb-{slug}        # Template path output (con trattino!)
BASE_DRIVE=G:/Drive condivisi/Nexty Docs          # Path base reale (modifica se serve)

# === Docker Preview ===
GITBOOK_IMAGE=honkit/honkit                       # Immagine Docker per la preview

# === GitBook API (solo se usata in fasi successive) ===
GITBOOK_TOKEN=gb_api_cVjXYtuWR7lytWd0GvfGalynSzxt6PQzJlXAxb1p
GITBOOK_WORKSPACE=next-2

# === Logging Strutturato ===
LOG_FILE_PATH=logs/onboarding.log                 # Percorso del file log persistente
LOG_MAX_BYTES=1048576                             # Dimensione max (in byte) per rotazione
LOG_BACKUP_COUNT=3                                # Numero di backup log da mantenere
DEBUG=false                                       # true/false per abilitare livello DEBUG

# === Esempio: altre custom env per future estensioni ===
# SLACK_WEBHOOK_URL=
# EMAIL_NOTIFY=

# =====================================================
#   ⚠️  Non committare mai questo file su repo pubblici!
# =====================================================
